apiVersion: v1
kind: Service
metadata:
  name: gophprofile-worker
  namespace: gophprofile
  labels:
    app: gophprofile-worker
spec:
  selector:
    app: gophprofile-worker
  ports:
    - name: metrics
      port: 9091
      targetPort: metrics
      protocol: TCP
  type: ClusterIP