apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: gophprofile
  labels:
    app: loki
spec:
  selector:
    app: loki
  ports:
    - name: http
      port: 3100
      targetPort: http
  type: ClusterIP